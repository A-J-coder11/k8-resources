apiVersion: v1
kind: Pod
metadata:
  name: env-ConfigMap
spec:
  containers:
    - name: nginx
      image: nginx
      envFrom:
        - configMapRef:
            name: pod-config  // refer from the 08-config-map.yaml metedata