apiVersion : v1
kind: Service
metadata:
  name: nginx
  labels:
    purpose: service-demo
spec:
  type: NodePort
  selectors:
    purpose: service-nodeport-demo
    project: projectk
    environment: dev
  ports:
    - protocol: TCP
      port: 80  # service port
      targetPort: 80 # container port

-----

apiVersion : v1
kind: pod
metadata:
  name: nginx
  labels:
    purpose: service-nodeport-demo
    project: projectk
    environment: dev
spec:
  containers:
  - name: nginx
    image: nginx